# PlotForge Static Player

The MVP static player is generated by `plotforge-export` as a self-contained `index.html` plus `game.json` and assets. A separate React player can replace this template later as long as it consumes the generated contracts in `contracts/`.
